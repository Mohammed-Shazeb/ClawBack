# 2026-09-21

## Hackathon submission assessment

User asked whether the app meets the Convex All Gas Hackathon requirements and for a full
end-to-end status. Deadline: 2026-09-22 12:00 PT = 2026-09-22 19:00 UTC = 2026-09-23 00:30
IST (~29 h from the start of this session).

**Findings:**

- **No git remote exists.** 8 commits sit locally on `master`, nothing pushed. No `gh` CLI on
  this machine, so the public repo needs the user to create it and hand over the remote URL.
  "Public repo" is a hard submission requirement and is currently unmet.
- **`hackathon.md` was stale** — it stopped at 2026-09-14. Since the build log is *the* thing
  judges read, I extended it with 09-17 (live provider verification), 09-18 (UI rebuild),
  09-19 (auth, new main UI, the two production defects) and 09-21 (where the app stands).
  Also marked the old "demo workspace identity … production auth needed" limitation resolved.
- **No hosting config of any kind** — the live URL requirement is unmet.

**Static-export feasibility (checked this Next version's docs, not assumed):**
`output: 'export'` is supported, but **"Dynamic Routes without `generateStaticParams()`" is
explicitly unsupported**. `app/cases/[caseId]/page.tsx` is exactly that. The new main UI
(`/case`, `/evidence`, `/intelligence`, `/letter`, `/timeline`) has no dynamic segments and
would export cleanly. So publishing on `convex.site` needs the case deep-link converted to a
query param, dropped, or accepted as 404.

Wrote `SUBMISSION-STATUS.md` — checklist, judging criteria, the export blocker, a full
start-to-end walkthrough of the app, the one real case run, the verification battery, and an
honest limitations list.

**Still on the user:** public repo, live URL (needs my go-ahead to start), 3-minute video
(I cannot generate video), Luma registration, social post, vibeapps.dev submission.
